

import spock.lang.Specification

class SeleniumSpec extends Specification {

    def driver
/*
    def 'Selenium IDE download link'() {
        given:
        driver = new RemoteWebDriver(new URL("http://localhost:4444/wd/hub"), new FirefoxOptions())
        //System.setProperty("webdriver.gecko.driver","C:\\Program Files\\Geckodriver\\geckodriver.exe")
        //driver = new FirefoxDriver()
        when: 'I input selenium in browser'
        driver.get("https://www.selenium.dev/")
        (JavascriptExecutor) driver.executeScript("window.scrollBy(0,250)", "")
        driver.findElement(By.xpath('//html/body/section[2]/div/div[2]/div[2]/a/div/b')).click()
        then:
        driver.getTitle() == 'Downloads'
        cleanup:
        driver.quit()
    }*/
}

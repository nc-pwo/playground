

import spock.lang.Specification

class Selenium2Spec extends Specification {
    def driver
/*
    def 'Some random docker link'() {

        setup:
        //System.setProperty("webdriver.gecko.driver","C:\\Program Files\\Geckodriver\\geckodriver.exe")
        //driver = new FirefoxDriver()
        driver = new RemoteWebDriver(new URL("http://localhost:4444/wd/hub"), new FirefoxOptions())

        when:
        driver.get("https://github.com/nikoly/selenium-grid-docker")
        driver.findElement(By.linkText("link")).click()
        then:
        driver.getTitle() == 'Install Docker Engine | Docker Documentation'
        cleanup:
        driver.quit()
    }*/
}
